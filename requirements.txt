# GAMEOVER EDITS — Python Dependencies
# Install with: pip install -r requirements.txt

# Core Telegram Bot Framework
pyrogram==2.0.106
TgCrypto==1.2.5

# Environment variable loading
python-dotenv==1.0.1

# Async HTTP (for future features)
aiohttp>=3.9.5

# For tracking async tasks cleanly
asyncio-throttle==1.0.2

# System resource stats
psutil==5.9.8

# AI Audio transcription (Whisper)
openai-whisper

# Google Drive API (Phase 1 — Drive Bridge)
google-api-python-client>=2.100.0
google-auth>=2.23.0
